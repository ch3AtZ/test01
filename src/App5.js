import React from "react";
import validator from 'validator'

validator.isEmail('test');
































/* const square  = (x) => x*x 

const add = (a,b) => a+b; // add export before const to export directly

const subtract = (a,b) => a-b;
export {square , add}
export default subtract ;

 */



